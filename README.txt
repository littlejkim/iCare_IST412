Run App in default packagae

If this is your first time running, please create your username and password. 


Design pattern interface (Navigation): implemented through JTabbedPane

O-O pattern implementation: implemented through Java DB (main controller takes care of queries). created JavaDB (SQL) to insert/update account details (need to work on it more).

Group work: implemented logging in user (user's tabs are disabled when user isn't logged on)